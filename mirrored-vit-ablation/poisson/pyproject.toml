[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tunable-rom-vit-poisson"
version = "0.1.0"
description = "Ablation: Tunable NM-ROM with symmetric ViT encoder + ViT decoder (un-patchify) + linear skip for the parametric Poisson equation in 2D and 3D"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
authors = [{ name = "Tahmid Awal" }]
dependencies = [
    "jax>=0.4.20",
    "jaxlib>=0.4.20",
    "flax>=0.8.0",
    "optax>=0.1.7",
    "numpy>=1.26",
    "scipy>=1.11",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
dev = ["pytest>=7.4", "pytest-xdist>=3.5"]

[project.scripts]
poisson-generate-data = "scripts.generate_data:main"
poisson-train = "scripts.train:main"
poisson-run-rom = "scripts.run_rom:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-dir]
"" = "src"
