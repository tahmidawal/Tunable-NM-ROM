# Lane protocol — 2026-09-17 ICLR data campaign (read fully before doing anything)

You are one of nine parallel agents. Each agent owns exactly ONE worktree and ONE cluster
namespace. Paper deadline: 2026-09-25 (ICLR 2027). Your lane's deliverable is a
source-generated report plus JSON so the coordinator can assemble paper tables. The user's
instruction: "get as much information as possible so that we can pick and choose what to put
in the paper", "have the agents iterate", "push for better results". Iterate until your
deliverable is as strong as the evidence allows, then report.

## Rules that are not negotiable (from CLAUDE.md / AGENTS.md of this repo — read them too)

1. Work ONLY inside your worktree `/home/tahmid/Dev/pod-ae-nmrom/Tunable-NM-ROM-Claude/worktrees/<your-lane>/`.
   Read any other worktree freely; NEVER write to one. Never `cd` to the main checkout to
   edit files; the only main-checkout write allowed is the lab-log append via the helper below.
2. Cluster namespace: `/cluster/tufts/paralab/tawal01/<your-namespace>/<attempt>/`. One job per
   attempt directory, never two. `squeue -u tawal01` BEFORE and AFTER every `sbatch`, and
   confirm exactly one job per attempt dir. Submit from `tufts-login` only. Partition `gpu`
   only, never `preempt`. Exclude `pax007` (fails cuInit).
3. Copy the mechanics of `worktrees/2026-09-16-q-ridge/experiments/q-ridge/cluster/{stage.py,collect.py}`
   and `runs/qrg304/run.sbatch`: stage files byte-checked against `git show HEAD:<path>`,
   write `COMMIT.txt`, `PROVENANCE.json`, `MANIFEST.sha256`; the sbatch must activate
   `/cluster/tufts/paralab/tawal01/ae-research/venv/bin/python`, export
   `JAX_ENABLE_X64=true JAX_DEFAULT_MATMUL_PRECISION=highest`, run the GPU preflight
   (`jax_backend=gpu` or exit 42), write `OUTPUTS.sha256`, end with `ALL-DONE`. All output
   under paralab, never `~`.
4. GPU choice: `--gres=gpu:a100:1` first. The queue is heavy today (~200 pending). If your
   job is still PENDING after 3 h, `scancel` it and resubmit as `h100`, then `h200`, then
   `l40s`. Never change the science between resubmits. Use `--mem 180G` on A100/H100;
   `--mem 240G` and H200 only for nc128-scale (n ≈ 262k, 20k samples) work.
5. After a job finishes: checksum-collect (`collect.py` pattern), verify hashes locally,
   run an INDEPENDENT NumPy audit that recomputes every reported error from saved fields
   without importing the driver or JAX, archive as bounded Git chunks in your worktree, THEN
   delete the exact remote attempt directory. Disk on paralab is 91 % full: do not park
   anything there.
6. Local GB10: every JAX process goes through `jaxrun` with the absolute venv python, and
   through the shared slot lock so nine agents never exceed three concurrent local jobs:
   ```
   source /etc/profile.d/jax-mem.sh
   JAX_DEFAULT_MATMUL_PRECISION=highest /tmp/claude-1002/-home-tahmid-Dev-pod-ae-nmrom-Tunable-NM-ROM-Claude/6e5fe858-5d6b-4b34-a1bd-df699b1d4032/scratchpad/jaxrun-slot jaxrun /home/tahmid/Dev/.venv/bin/python <script> ...
   ```
   Local runs are for sub-minute smokes only. Anything real goes to the cluster.
   Never bare `python`/`python3`. Never `pip install` without `--no-deps`.
7. Before the FIRST cluster job: commit `experiments/<lane>/DESIGN.md` with the question,
   the pre-registered pass/fail criterion, the arms, the gates, and the falsification clause.
   Amend by appending §A1, §A2… — never rewrite history. Run a local smoke that reproduces
   an audited baseline number from the parent lane to ≤1e-9 before submitting.
8. Every reported number is generated by a script from run JSONs into
   `experiments/<lane>/reports/2026-09-1x-<lane>.md` + a machine-readable
   `experiments/<lane>/reports/summary.json` (one object per table row: arm, metric, value,
   job id, source SHA). Never hand-type a number into prose. Every table ends with a
   plain-language glossary. Equations LaTeX, diagrams mermaid.
9. Timing: three timed repetitions with burn-in, device sync, medians reported with all reps
   retained. NEVER form a ratio across jobs or GPUs; same-job controls only. Errors: worst
   over cases (and median), against the converged same-grid full-order solve timed in the
   same job. Burgers: report worst-over-evolved-times AND worst-over-all-times AND the t=0
   compression separately.
10. Commit early and often in your worktree (`git add -A && git commit`). Do NOT push
    (pushes of this 200 GB repo fail and repack on the shared box; the mirror decision is
    the user's). Do NOT merge. Do not touch `best-results/`.
11. Lab log: at the END, write your dated entry to a file and append it with
    `/tmp/claude-1002/-home-tahmid-Dev-pod-ae-nmrom-Tunable-NM-ROM-Claude/6e5fe858-5d6b-4b34-a1bd-df699b1d4032/scratchpad/lablog-append <entry-file>`
    (it takes a lock; nine agents share the log). Format: `## 2026-09-1x` then
    `### <lane> — <one-line verdict>`, then: what ran (branch, commit, job ids, GPU,
    namespace), what was found with numbers, WHAT WAS WRONG AND RETRACTED, what is open.
    Never edit LAB-LOG.md any other way.
12. Use Codex (OpenAI gpt-6-astra, a different model family) as an independent auditor at
    two points: (a) audit your DESIGN.md before the first job; (b) audit your final report
    against the raw JSONs. Also use it when stuck for ideas. Always headless, read-only,
    prompt on stdin, background if long:
    ```
    codex exec -s read-only -C <your-worktree> -o experiments/<lane>/reports/codex-<what>.md - < prompt.txt
    ```
    In the prompt say explicitly: do not modify, create or delete any file; do not run GPU
    work; do not submit jobs. Read its output and act on real findings; record which
    findings you accepted or rejected and why.
13. Job cap per lane: 8 cluster jobs. Preamble deaths with zero GPU time do not count.
    Retracted jobs still count. If you need more, say so in your final report.
14. Landmines already paid for: `engines.params_draw` draws column-by-column (drawing 4
    cases ≠ extending 2); cluster vs GB10 NumPy differ by 1 ulp in `exp` (value gates, not
    hash gates, across machines); the GB10's first f64 QR of a 64516×512 matrix can return
    NaN (rank-assert); a job holding N jitted reduced queries holds N executables on
    device (14 fit a 40 GB A100, 34 do not); never close a jit over a training array (pass
    it as an argument); NNLS fit residual does NOT certify an EQ rule, only held-out ρ on
    reachable states does; an empty cluster log usually means disk-full, not a code bug.

## How to report back to the coordinator (your final message)

1. Verdict in one sentence.
2. Table(s) of the numbers with job ids, exactly as `summary.json` holds them.
3. What was retracted or went wrong, honestly.
4. Paths: DESIGN.md, report .md, summary.json, codex audit files, archive chunks.
5. What you would run next with more time, and the job count used.
The coordinator (a Claude session) reads only your final message; make it self-contained.

## Coordinator notice, 2026-09-17 ~23:30 — Codex quota exhausted until 2026-09-19 11:33 (verified)

`codex exec` returns "You've hit your usage limit ... try again at Sep 19th, 2026 11:33 AM". Do not
retry before then; do not block on it. Substitute a written self-audit (a `reports/self-audit-<what>.md`
listing each claim, the JSON field it rests on, and the check you ran) and record the substitution as a
DESIGN.md amendment. After 2026-09-19 11:33, run the Codex report audit if your lane is still open.
Also: pass `--skip-git-repo-check` only if Codex refuses to start; worktrees are inside the repo, so it
should not be needed.

## Coordinator notice, 2026-09-17 10:25 — session restart, agents respawned on demand

The coordinator session restarted and every lane agent was lost. Cluster jobs were unaffected and kept
running; all lane worktrees are committed and intact. Two changes to how lanes now run:

1. **Do not idle on monitors.** When your remaining work is blocked on a cluster job, stop and return a
   report. The coordinator re-invokes you when your jobs land. Idle polling consumed most of the previous
   budget and the account has already hit a model-usage limit once.
2. **A respawned agent inherits nothing but the repo.** Everything that matters must be in your
   `DESIGN.md`, your committed code, your `reports/summary.json` and the lab log — not in conversation.
   This is the project's existing rule and the restart is what it exists for.

## Coordinator notice, 2026-09-17 18:05 — third restart; the paper's Markdown source is canonical

Another restart (usage limits) lost every lane agent; cluster jobs were unaffected. Two lanes (b-qxm,
b-lowvisc) were killed with uncommitted edits: start with `git status` and `git diff`, keep what is sound.
The paper's canonical source is `worktrees/2026-09-16-paper-refresh/paper/PAPER.md` (the external doc
mirror is gone). Usage limits are recurring: do your lane's work in as few turns as possible, commit
after each step, and stop as soon as you are blocked on a job.
