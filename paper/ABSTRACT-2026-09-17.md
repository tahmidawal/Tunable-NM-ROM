# Title and abstract for the ICLR 2027 abstract deadline (2026-09-18 AOE)

## Current claim state — 2026-09-20 claim-scope repair and development supplement

The current manuscript is `main.tex`; its title is unchanged. The abstract now scopes
operator operating points to a fixed grid and inference procedure, acknowledges that
resolution can change error and cost, and identifies the scheduled ladder as one frozen
decoder with a prescribed test-count schedule. These are wording corrections; every
measured scientific value remains unchanged. Everything below this current-state section
is dated history, including
earlier proposed titles, abstracts, completeness claims and subsequently retracted readings.
The separate three-dimensional campaign appears only as a provisional internal-draft
appendix; it supplies no new abstract or main-text performance claim.

| Claim or evidence | Current manuscript treatment |
| --- | --- |
| Low-viscosity Burgers provenance | The corrected summary is pinned to `5760a7e256ef3c003956bef7a185a86c88b823b6`. Gate, training and panel commits are displayed separately. Historical incumbent training rows with unknown source commits retain their artifact hashes; the under-resolution and failed orthogonality-audit exceptions remain explicit. No scientific value changed. |
| Navier–Stokes capacity diagnosis | The causal claim that more data established a shift from generalisation to capacity limits is withdrawn. Training and held-out reconstruction errors use different normalisations. Their historical ratios are qualified; matched held-out head/POD comparisons remain supported as achieved best-found fits. Budget exits prevent treating the gate failure as a global-optimum impossibility result. |
| Navier–Stokes ladder coverage | The exploratory phase-three ladder after the failed phase-two gate exists and remains labelled exploratory. A confirmatory ladder on a passing head was not run. Literal claims that no phase-three job existed are superseded. |
| Poisson collapse criterion | The manuscript displays literal and amended falsification readings. The coarse-mesh literal criterion fails because the linear endpoint is cheaper, while the amended interpretation does not falsify collapse. Amendment A8 followed coarse observations and preceded the fine job; it is not described as fixed before all observations. |
| Poisson reconstruction columns | Historical nonzero-correction best-found columns retracted under A10 remain excluded from their original tables. A separate corrected full-cohort rerun is independently confirmed and generated in the new oracle appendix. The historical solved ladder and direct-QR endpoint are unchanged. New evidence is pinned in `tables/campaign-provenance.json`; finite best-found errors remain upper bounds on the global minimum. |
| Wave accuracy comparison | The literal top-rung accuracy criterion fails; the empirical tie-band criterion passes. A2 introduced that band after a local pilot and before cluster jobs. It is neither a uniform trajectory integration bound nor a consequence of energy conservation. |
| Deployment-claim scope | A frozen operator contributes one point at fixed grid and inference procedure; resolution may change error and cost. The scheduled ROM ladder uses one frozen decoder with a test-count schedule. Correction rank changes the trial space at fixed grid and test space; numerical settings can also change error. |
| Linear comparison and physical error | Linear projection restricts the full-order equations; universal inheritance of guarantees is not asserted. Head superiority is restricted to achieved comparisons with the fitted affine/POD controls. Discretization limits physical-error gains without making those gains identically zero. |
| Neural-operator accuracy | Values are achieved finite-budget errors. The old lower-bound wording is withdrawn. Checkpoint recency is a heuristic, not evidence of convergence or guaranteed further improvement. The historical precision exception, mean-validation selection rule, seed controls and operator resolution knob remain disclosed. |
| Timing and test data | Timing comparisons remain within allocations. The three-dimensional appendix contains audited development panels selected explicitly in its frozen source manifest. No final cohort was opened, no pilot advantage is promoted to a main claim, and failed operator variants and classical controls remain displayed. |

Validation is recorded by `check_integrity_repair.py` in
`integrity-repair-2026-09-20.json`: it compares existing scientific macros and tables to
the exact prior manuscript commit, rebuilds that baseline with the same TeX environment,
checks for new overfull boxes and undefined references, and checks the submission page limit.
The added linear-protocol table is generated from retained verdict records, and source
pins are recorded in `tables/provenance.json`. The subsequent supplement's preservation
and build checks are in `campaign-integration-2026-09-20.json`; its snapshots are refreshed
only by an explicit committed-source argument to `gen_campaign_supplement.py`.
The six source-review findings and exact before/after wording are recorded in
`claim-scope-disposition-2026-09-20.json`; its Markdown companion explains their scope.
The updated integration check explicitly verifies the permitted abstract changes,
unchanged title, original scientific macros/tables, source pins and page limit.
The development snapshot includes checkpoint-derived bank/head architecture and
completed-update records; it still supplies no accepted final-test comparison.
No numerical experiment ran in this manuscript worktree.
Existing source-report audit dispositions outside the paper worktree and the ongoing
three-dimensional experiments remain separate work. No worktree was merged or pushed.

### Layout follow-up in the same repair

The inherited sampling-family table had unwrapped source paths extending beyond its
cells and was scaled to an unreadably small font. Its generator now wraps the paths
and uses columns sized for the page at a readable font size. The experiment map and
architecture block table wrap within the text width, the heat table fits the page,
and a small equation-spacing overrun is removed. Generated Markdown cells remain
identical; the integrity check distinguishes these formatting edits from data changes.
The affected PDF pages were rendered and visually inspected. The problem-table
caption now correctly records the opened Burgers sealed cohort, and the provenance
caption retains the historical operator precision exception.

## Historical record begins here

Provisional text agreed in-session on 2026-09-17. It carries **no performance ratios** on purpose:
every number is single-seed, development-cohort, one checkpoint per PDE until the `b-seeds`,
`b-panel` and `b-qxm` lanes (launched 2026-09-17) land. Titles can be edited until the full-paper
deadline (2026-09-25). Positioning: the rejected NeurIPS paper's three-contribution skeleton
(tunable NM-ROM from one decoder / the architecture that makes it possible / the implementation),
re-founded on the current bank + head + correction decoder; the knob is the correction rank $q$;
Burgers is the hero; Poisson and heat are the cases where the curve collapses to a linear model;
no speedup over an efficient full-order solver is claimed.

## Title

**Tunable Nonlinear-Manifold ROMs from One Trained Decoder**

Alternative: *Tunable Nonlinear-Manifold Reduced-Order Models from a Single Trained Decoder: Bank,
Head, and Correction Rank*

## Abstract (about 200 words)

A learned PDE surrogate usually gives one accuracy–cost point; getting another means retraining.
We show a nonlinear-manifold reduced-order model that turns one trained decoder into a whole
family of operating points chosen at run time. The decoder writes the solution as a fixed spatial
bank times a small neural head. At inference, the solver can add $q$ extra bank directions on top
of the head's output; $q$ sets accuracy, while precomputed quadrature rules and the solver
tolerance set cost. Three ingredients make this work: a bank that evaluates per node, so cost is
flat in mesh size; a linear skip in the head, so the solve starts reliably; and the split between
bank, head, and corrections, so the rank dials between the head's compression and the bank's
floor. On 2D Burgers the family is monotone, meets a bar fixed before any run, and beats linear,
POD, and FNO alternatives at matched size. On Poisson and heat the family collapses to a linear
model, which tells you when the nonlinear manifold is worth having. We also show that quadrature
rules must be checked on states the solver actually visits, not on their fit residual. The model
does not beat an efficient full-order solver in 2D.

## Binding corrections from the 2026-09-16 Codex critique (keep out of the abstract until measured)

- "beats ... POD ... at matched size" is an accuracy statement; cost dominance over POD is
  cross-job today and POD-32…128 were unconverged in the only same-job panel (`b-panel` fixes).
- "FNO" was budget-matched among FNO sizes, not against the ROM: say "trained on the same data".
- ~~The passing dense ladder uses $M=4(K+q)$; rank and test count are confounded.~~ **RESOLVED 2026-09-17 by `b-qxm`** (jobs 3780175/3780177/3780178, crossed grid, dense, budget 600, one A100 per block).
  At **fixed $M=1088$** the pure-rank ladder $q=0,64,128,256$ is monotone, every rung converged, and it
  **passes the pre-registered tunability bar on its own**: error span $2.44\times$, cost span $5.16\times$,
  four non-dominated points, all measured inside one job. So the paper should headline the **fixed-$M$
  ladder** and report the scheduled one beside it. Supporting decomposition: on the balanced sub-grid the
  rank main effect is $85.1\,\%$ of the variance in log error against $6.1\,\%$ for the test count; the
  scheduled ladder's span is $69\,\%$ rank / $31\,\%$ test count, and the test-count share falls from
  $83.5\,\%$ at the first rung to $4.6\,\%$ at the last. **Corrected by the lane's final report (do not repeat my earlier
  wording):** the test count does **not** simply saturate. At $q=0$ it is flat past $M^\star=256$
  ($1.2710\to1.2649\,\%$ out to $M=4096$) but that flat stretch still costs $1.21\times$, so it is not
  cost-neutral; and at $q=256$ the $M$ effect is still $2.025\times$.
  **RETRACTED by the lane itself:** its interim claim that *tests per unknown* is the invariant does
  **not** hold — it compared a 4x-16x range at $q=0$ against a 2x-8x range at $q=256$ and generalised
  from mismatched ranges. Measured: saturation sits near **16** tests per unknown at $q=0$, near **4**
  at $q=64$, and is **not reached by 8** at $q=256$, so the absolute $M$ needed **grows with $q$**.
  Job 3783899 replaces it with a measured curve. No invariance statement goes in the paper.
  The split is also not uniform along the ladder: the test count does
  $84, 77, 40, 10, 5\,\%$ of the work at successive rungs, i.e. **the test count buys the bottom of the
  ladder and the rank buys the top**. The inner-solver switch at $K+q>64$ is inert (Gauss--Jordan vs LU
  agree exactly). Honest framing for the paper: fixed-$M$ **isolates** the rank effect, while the
  scheduled rule is the **cheaper operating family** — the fixed-$M=1088$ ladder's cheap end costs 848 ms
  against the scheduled ladder's 291 ms at $q=0$, so headlining pure rank means quoting a dearer baseline.
  Best accuracy seen anywhere: $0.3736\,\%$ at $(q=256, M=2176)$ on the $8(K+q)$ schedule.
- EQ rules certify on the primary bar only for $q\le64$ (`b-eqtop` fixes).
- No ratios (3.6×, 13×, 5.9×, 1.52×, "parity at 1024²") until seeds and the sealed cohort exist.

## Evidence landed 2026-09-17 (coordinator notes)

- **Frontier, 256^2, one allocation, 35 timed subjects (`b-panel`, job 3780638).** **Zero of 27 reduced
  subjects are non-dominated** once the same-job full-order controls are included: Newton at `ntol 1e-3,
  dt 0.005` reaches 0.0489 % at 31.4 ms and `ntol 1e-4` 0.0338 % at 36.7 ms, against the best reduced arm
  (dense q=256) at 0.5194 % / 4164.6 ms. The abstract's sentence "does not beat an efficient full-order
  solver in 2D" is now backed by one-allocation evidence and should stay.
- **Empirical quadrature is a real, same-job cost lever at equal error.** At matched (q, M): q=0/M=64
  dense 284.8 ms / 1.8890 % vs certified EQ 57.9 ms / 1.8891 %; q=128/M=576 dense 1223.2 ms / 0.8930 % vs
  EQ 247.0 ms / 0.8925 %. Roughly 5x cheaper at four-decimal-identical error. Loosening the tolerance from
  1e-6 to 1e-3 removes a further ~25 % at negligible error change.
- **Open, must not ship unresolved:** the POD-LSPG arms at k' >= 32 are flagged not converged (final
  gradients 1.8e-1 down to 4.5e-2) while POD-512 prints 0.2184 % at 2928.9 ms, i.e. better than the best
  ROM rung on both axes. Either converge them under the shared rule or say plainly why they cannot.
- **EQ top-rung certification (`b-eqtop`, job 3780164, in flight).** The parent lane's blocker was the
  **number of fit states, not m**: at q=256 a rule with 64 fit states certifies on the primary bar at
  m=2048 (rho_max 0.1074) where the 8-fit-state rule gave 0.1678. `q-ridge`'s m ~ 2522 extrapolation was
  fitted through draw noise. Treat as provisional until the draw-replication job (3783811) bounds that noise.

### CLAIM CHANGE — the neural-operator comparison (`no-second`, jobs 3780138 / 3780139)

The abstract currently says the family "beats linear, POD, and FNO alternatives at matched size".
**The neural-operator half of that is no longer defensible.** A PDEBench-style U-Net and a Transolver,
trained on the *same* data, split, reference, metric, wall budget and selection rule as the FNO, beat the
ROM on the matched eight-case diagnosis cohort (worst relative error, accuracy is comparable across jobs
on these cases; timing is not, so no speed claim is attached):

| method | worst % |
|---|---|
| `unet-small` | 1.4712 |
| `unet-medium` | 1.5189 |
| `tsol-refine` | 1.5224 |
| `unet-refine` | 1.7110 |
| **`rom`** | **1.8671** |
| `fno-large` | 2.4829 |

So the ROM beats the FNO and loses to three U-Net capacities and one Transolver capacity. The earlier
"more accurate than a tuned FNO on Burgers" was true only because the FNO was the weakest of the three
operator families. Neural operators are also far cheaper per query, so on Burgers at 256^2 they dominate
the ROM on both axes.

**What survives:** the ROM beats POD-LSPG at matched latent dimension (a within-reduced-model claim), and
the tunability contribution is unaffected, since each trained operator still gives one operating point.
**What must change:** drop any claim of accuracy superiority over neural operators; say plainly that
well-tuned operators are more accurate and cheaper here, and that the reduced model's case rests on the
mechanism, the structural condition, and residual-based physical consistency rather than on winning.
This is exactly why the second operator was worth running: shipping with an FNO-only comparison would
have left the paper open to a reviewer who trains a U-Net.

## Abstract, revision 2 (supersedes the text above; ~205 words, no ratios)

**Title unchanged:** Tunable Nonlinear-Manifold ROMs from One Trained Decoder

A learned PDE surrogate usually gives one accuracy-cost point; getting another means retraining. We show
a nonlinear-manifold reduced-order model that turns one trained decoder into a family of operating points
chosen at run time. The decoder writes the solution as a fixed spatial bank times a small neural head. At
inference the solver may add $q$ extra bank directions on top of the head's output: $q$ sets accuracy,
while offline-certified quadrature rules and the solver tolerance set cost. Three ingredients make this
work: a bank that evaluates per node, so cached cost is flat in mesh size; a linear skip in the head, so
the solve starts reliably; and the split between bank, head and corrections, which lets the rank dial
between the head's compression and the bank's floor. On 2D Burgers the family is monotone and meets a bar
fixed before any run, with the test count held constant so the rank alone is the control, and it beats
the classical linear reduced model at matched latent dimension. It does not win outright: a well-tuned
full-order solver, and neural operators trained on the same data, are both cheaper and more accurate
here, and we report that. On Poisson and heat the family collapses to a linear model, which says when the
nonlinear manifold is worth having. We also show that quadrature rules must be validated on states the
solver actually reaches, not on their fitting residual.

### Frontier arithmetic — corrected twice, final

All-times metric, **reduced subjects only**, the eight non-dominated points:

| subject | family | GPU ms | worst all-times % |
|---|---|---|---|
| `q0_M64_eqcert_g1em06_fastL4` | fast kernel | 39.1 | 2.5629 |
| certified EQ $q=0$ | rom | 42.6 | 2.5628 |
| certified EQ $q=16$ | rom | 60.4 | 2.4806 |
| certified EQ $q=32$ | rom | 72.3 | 2.3534 |
| certified EQ $q=64$ | rom | 88.5 | 2.1489 |
| certified EQ $q=128$ | rom | 189.0 | 1.8116 |
| certified EQ $q=256$ | rom | 429.4 | 1.0324 |
| `free512_M1024_dense` | unrestricted bank | 2542.3 | 0.6027 |

**`pod512` is NOT on this frontier**: it is dominated by the unrestricted-bank endpoint, which is both
cheaper and more accurate (2542.3 ms / 0.6027 % against 2928.9 ms / 0.6125 %).

Two corrections were needed to get here, both worth recording because they show how the same file
yielded two different frontiers:

1. **The lane's** interim sentence "the certified-EQ ladder owns the *entire* reduced frontier" was wrong.
   It owns **seven of eight**; the eighth is the unrestricted-bank endpoint, which is not a ladder rung.
   Its "no POD rank on it" clause was **correct**.
2. **Mine** was worse: I re-derived the frontier with a candidate set filtered to the `rom` and `pod`
   families, which silently dropped the `free` and `fast` families, and concluded POD-512 was on the
   frontier. It is not. The lane's generator had the mirror-image defect — it enumerated only ladder
   rungs and POD ranks — and now enumerates every family on each frontier, so generated text cannot hide
   a member again.

**On the evolved-times metric POD-512 *is* non-dominated**, and there it beats the best dense
correction rung on both axes (0.2184 % at 2928.9 ms against 0.5194 % at 4164.6 ms). So the head
ablation's "no POD rank up to $k'=128$ matches the neural head" holds but does not extend to $k'=512$.
This is the metric choice mattering for a comparison, and the paper must report both.

With the full-order controls and the FNO included, **no reduced subject is non-dominated on either
metric**. That headline is unaffected by either correction.

### T14 follow-up — every operator number is a LOWER BOUND on operator quality

`no-second` closed 2026-09-17. Two facts the paper must carry with the table:

1. **Nobody converged.** Seven of the lane's eight Burgers arms and **four of four FNO arms** were still
   improving when the wall budget ended. So every operator error quoted is a lower bound on how good
   that family gets with more compute, ours included in the sense that a reviewer with a bigger budget
   would beat us by *more*, not less. State this beside the table, not in a preamble.
2. **The FNO was simply the weak family.** Every FNO capacity is worse than the ROM; four of the eight
   U-Net and Transolver arms beat it. On Poisson the gap is larger still: U-Net 6.02/8.31/12.80 % worst
   against FNO 28.3-32.5 %.

The lane's own report audit caught six honesty defects before publication, including a timing table that
pooled two jobs under a "same-job" heading — the exact error class this project has retracted before —
and a pre-registration audit caught a gate that would have failed every float32 arm (errors from a
batch-8 pass, saved fields from batch-1, gated at 1e-11; the measured gap reached 2.5e-6).

**Still open and binding:** `res01` tests whether a trained operator exposes its own accuracy-cost family
through evaluation resolution, at 256/128/64/32 intervals with an interpolation-floor control and
same-job per-rung timing. Pre-registered falsification: if any operator clears 1.5x speedup at no more
than 2x its own error, then "one accuracy-cost point per trained operator" is **false and is withdrawn
from the paper**. That sentence is currently in the abstract's opening, so this one job can force an
abstract change.

### T11 wave rows — FINAL (`w-ladder`, jobs 3780447 / 3783805 / 3780450, three meshes)

The reflective 2D wave is the **second linear PDE with a degenerate ladder**, same shape as heat. The top
rung is the full learned bank evolved linearly with no neural head, and it is both the most accurate rung
and far the cheapest.

| mesh | cheapest head rung | top rung (bank, no head) | POD at matched rank 64 | direct solve |
|---|---|---|---|---|
| 64² | 11.205 % / 184.5 ms | **4.932 % / 1.168 ms** | 1.489 % / 1.202 ms | exact / 3.391 ms |
| 256² | 11.331 % / 183.5 ms | **5.121 % / 1.428 ms** | 1.502 % / 1.399 ms | exact / 4.356 ms |
| 1024² | 11.339 % / 192.5 ms | **5.133 % / 4.562 ms** | 1.501 % / 4.232 ms | exact / 17.103 ms |

Structure worth stating in the paper: **error is monotone in $q$ but cost is not**. Cost rises from 185 ms
to 2500 ms across the middle rungs and then *collapses* to 1.17 ms at $q=R$, because the top rung deletes
the nonlinear solve entirely. That is the degeneracy made mechanical.

**A harder negative than the head result.** POD-Galerkin **beats the learned bank at matched rank 64 at
every mesh** (1.49/1.50/1.50 % against 4.93/5.12/5.13 %, at the same cost). So on waves it is not only the
nonlinear head that is dominated — the learned bank itself is worse than a POD basis of the same rank.
The lane flags that this bears on every cell that uses this bank. No reduced arm beats the direct solve;
the one place the bank wins is against the coarse-mesh full-order solve at 1024² (5.13 % / 4.56 ms against
6.81 % / 13.06 ms).

**Retracted in-lane:** a hand-typed cross-job timing in its own design document; the pre-registered
orthonormal correction directions (they force the Cholesky guard into fallback, costing ~10x, which is a
solver artefact and not the price of enrichment); and a retained-value gate that was wrong twice and
killed job 3780448 *after* it had completed all 432 invocations — the cause was tie-breaking in `argmin`
across eight cold-fit starts that reach the same minimum, not a numerical defect.

### T18 L-shaped Poisson — bank and head layers landed (`lshape`, job 3783786); solve jobs queued

**The re-entrant corner is not what limits the decoder.** The bank clears its pre-registered 1.0 % floor
(0.6650 % worst on the development cohort, 1.6155 % on the 256-source common cohort) and the L-shape
costs the bank essentially nothing against the square (0.7419 %). The exact $r^{2/3}$ and $r^{4/3}$
enrichment columns buy only 1.067x, so the pre-registered falsification (corner-limited bank) did not
trigger. Smooth R-function factor versus signed-distance factor: within ~1.1x on worst error, cohorts
disagree on the ordering, and the SDF's medial-axis kinks cost it 2.3-2.6x on the median. Reported as a
disagreement, not a pick.

**The head misses its 1.2x bar on every arm (2.2-5.8x above the floor)**, exactly as the parent square
cell does (4.2-4.5x). Every one of 32 solves exits stationary, so the head trains on the corner; it
simply does not close the gap. This is the fourth Poisson cell to show the head is function-class
limited. **Caveat that must sit beside the headline:** validation worst on 461 cases is 10.7-16.7 %
against development worst on 32 cases of 3.5-6.8 %; the 32-case cohort understates the tail.

**Retracted in-lane:** the independent audit's bank-floor gate (1e-8 relative) was wrong, not the floors.
The driver-vs-audit gap tracks each bank's condition number across four decades and is a constant
multiple of a one-epsilon feature perturbation: the cross-machine `exp`/matmul landmine amplified by
conditioning. Bound made absolute at 1e-6; measured worst 4.7e-8; no floor, ranking or selection moved.

**Open:** whether any reduced arm is non-dominated once a sparse direct solve is in the same job. Three
solve jobs (N=64/128, 256, 512) plus a q=R rung at M=513 are queued behind the GPU cap. That row is the
whole point of the cell and is empty until they land.

### Navier–Stokes (`ns2d`) — CLOSED as a pre-registered negative; phase 3 not submitted

Job 3787319 (ns203, K=16, R=256, full-rank bank, A100). Every phase-2 gate passes except
H-ORACLE: the head's held-out oracle median is 1.19× better than POD-16 against a pre-registered
bar of 2.0 (DESIGN §A4), so no ladder and no timing were run. Bank floor matches POD-256
(ratio 1.08); the limit is head generalisation (held-out/training gap 4.0; the head beats POD-16
by 1.63× at t=0 but 1.19× on evolved states, which depend on fourteen parameters that 512
trajectories sample at ~1.6 points per axis). ns202 (job 3783797, K=32 on a rank-capped bank) is
retracted as a pre-§A4 attempt and is not cited. ns204 (job 3787320, K=32 on the full-rank bank)
is the only pending NS item.

**Claim change.** The structural condition now reads: the correction-rank trade needs *both* a
residual nonlinear in the coefficients *and* a manifold that beats POD held-out. Poisson, heat
and waves lack the first; Navier–Stokes at this data budget lacks the second; Burgers on the
square has both and still loses on cost; the L-shape has both and wins on cost. Nothing about NS
timing or a ladder is claimed. The abstract's sentence about where the family collapses is
unchanged (it names Poisson, heat and waves, which collapse for the linear reason); NS is
reported in §5.6 as a different collapse, not added to the abstract.


### Reader-ready pass, 2026-09-17 evening — claim changes in one commit

**b-panel closed (lane commit 13ddecac).** bpn301 (job 3789570) replaces bpn101 wholesale at 256²
(bpn101 archived, not withdrawn); bpn203 (job 3789572, H200) adds 1024². The claim "nothing reduced
is on the frontier" is now **mesh-qualified**: 0 of 39 admissible reduced subjects at 256²; at 1024²
5 of 20 are non-dominated on (GPU ms, worst evolved %) — the q = 0/16/32 rungs with rules
transferred from 256² — and 0 of 20 on worst-all-times, because the t = 0 compression term bounds
that metric. Same-job ratio of the cheapest admissible reduced query to the cheapest full-order
setting: 4.48× at 256² → 1.82× at 1024². Not called a crossover; bpn401 (512², job 3805065) is
queued to bracket it. Abstract sentence "both cheaper and more accurate here" → "at $256^2$"
(the opening two sentences are untouched). The 1024² transferred rules at q ≥ 64 miss the primary
bar; cause open. Correction to earlier framing: the replication-selected q = 64 rule is confirmed
(2/2); it is the b-eqtop-ladder q = 64 rule (m = 1024) that is marginal.

**L-shape closed (lane commit dc762ed3).** lsh07 (job 3789568, 512²) landed; nothing retracted.
Head q = 64 is on the non-dominated set at 256² (2.77× cheaper than SuperLU, 2.131 %) and 512²
(7.60× cheaper, 2.123 %), the most accurate reduced subject at 512²; never more accurate than the
direct solve. **Now in the abstract and the conclusion** (the 512² rule was met); Contribution 3
cites both meshes. Nothing claimed beyond 512². Note for the coordinator: the L-shape is a Poisson
cell, so its residual is linear in the coefficients; the paper describes it as the case where the
full-order solve has no fast transform and the manifold beats POD at the same cost, not as an
instance of "nonlinear residual plus manifold".

**b-qxm regeneration committed (lane commit b4e38103).** The pin at 4b9723e8 is dropped; the
regenerated fixed-M = 1088 span is bit-identical (2.44× / 5.16×), so no number in §5.2 moved, only
the provenance commit. The working-tree contradiction was a lane generator defect and is recorded
in the lane's own docs, not in the paper. Round 2: q = 512 at M = 1088 did not converge (reported
as uninformative, not plotted, in no span); M-saturation at q = 256: M* = 2176 (8 tests per
unknown), diminishing return past it. Five jobs now in provenance.

**b-seeds development cohort committed (lane commit e533b48e).** T12 generated: three seeds, jobs
3783776/3783777/3783778; monotone on 3 of 3 (both metrics), knob bar 2 of 3, every rung converged
2 of 3 (seed3 at q = 256 exhausts the 600-step budget; budget, not divergence). Read from the
committed summary: the incumbent is *better* than every seed at q = 16–128 on the evolved metric,
so the paper does not say "incumbent inside the seed spread at every rung"; it says the incumbent
reproduces on 3 of 3 seeds and the spread is widest at q = 0. Sealed cohort (job 3804465) pending;
the abstract carries no ± number until it lands. Limitations §6 first paragraph rewritten.

**b-lowvisc** register-only (lvt01, job 3804337); no prose.


### Title change (user-approved, 2026-09-17) and the rebuild from the old source

**Old title:** *Tunable Non-linear Manifold ROMs for Elliptic and Parabolic PDEs via Matrix-Free
Galerkin Projection.*
**New title:** *Tunable Non-linear Manifold ROMs for Elliptic, Parabolic, and Hyperbolic PDEs via
Matrix-Free Petrov--Galerkin Projection.* Two-word change, everything else verbatim. Rationale
(user): Burgers and the wave cases add hyperbolic (advection-dominated) problems; the residual is
a weak least-squares Petrov–Galerkin projection onto sine test modes, not Galerkin; matrix-free
still holds. Wherever the old text said "Galerkin projection" for the method itself it now says
"Petrov--Galerkin projection" in the same sentence shape; citations to Galerkin ROMs in Related
Work are untouched.

**Rebuild (user direction, verbatim: "keep the wording and the writing style and even the title of
the previous paper ... just update with the current architecture and the new results").** `main.tex`
is now built from `old-neurips-main.tex`: same section order (Introduction; Related Work;
Methodology with its four subsections; Implementation; Experimental Setup and Benchmark Problems;
Numerical Experiments and Results; Conclusion and Future Work; appendices), same voice, old
sentences kept wherever still true. Sentences rewritten only where they described the ViT/LinearCP
decoder, or stated a claim the new results overturned, or a reviewer requirement needed one added
sentence. `PAPER.md` is regenerated from the new `main.tex` (the inline bibliography is not
rendered there). The abstract keeps the old opening two sentences about neural operators; its
knob sentence is $q$ plus the three solver knobs; its final claims sentence states what is true now
(monotone on three seeds, quadrature validated on reachable states, cost-only wins on the L-shape
at 256²/512² and at 1024² Burgers on evolved times, nothing reduced on the frontier at 256²,
where the family collapses). No ± number until T13 lands.

**Old claims replaced (each in the same voice and sentence shape):**
- "324× wall-clock speedup" / "12× to 191× speedup over CG" → no speedup claim; same-job ratios
  only (L-shape 2.77× / 7.60× cheaper than SuperLU; 1024² cheapest-reduced 1.82× the cheapest FOM).
- "strictly faster than full-order conjugate gradient" → "where a fast transform applies [FOMs]
  are hard to beat on wall-clock"; nothing reduced on the frontier at 256².
- "matches or beats FNO, DeepONet and SMA-NM-ROM on accuracy in most cells" → U-Net and
  Transolver beat the ROM; every FNO capacity is worse; DeepONet and SMA are not run under the
  protocol and not compared (limitation).
- "Pareto-dominates both baselines on 10 of 11 cells" and every "Pareto frontier/curve" →
  "family of operating points" / "non-dominated set"; the dominance claim is withdrawn.
- "three solver-side knobs (GN cap, tolerance, EQ samples) continuously trade accuracy" →
  the rank $q$ is the primary knob that moves accuracy; the three solver knobs move cost.
- "ViT encoder feeding a LinearCPDecoder (linear skip + shallow MLP + CP head)" → separable
  decoder: Fourier-feature coordinate-network bank × head with linear skip + nested corrections;
  no encoder in the deployed path.
- "binary mask + lift $u_g$" → smooth vanishing factor folded into the bank, zero lift.
- "Galerkin projection by $J_D^\top$" / "backward Euler for heat" / "CG on the projected operator"
  → LSPG onto fixed sine tests; Crank–Nicolson; direct solve of the damped normal system.
- Old Tables 1–4 (operators, best configs, knob directions) dropped; replaced by the generated
  T3/T5 (panel), T4 (rank vs tests), T8 (solver knobs), T14 (operators) and the rest.
- Old appendices (per-cell ViT training schedule; SMA/CNN-vs-ViT evidence) replaced by the
  generated method details, training-schedule pointer, architecture evidence, provenance, full
  tables and glossary.


### ns2d closed (lane commit 50bf36da): the NS negative covers both heads

ns204 (job 3787320, K=32, R=512) also fails H-ORACLE: oracle/POD-32 median 1.15–1.16 at all three
meshes (bar 2.0); every other gate passes. At t=0 POD-32 beats the neural manifold (POD/oracle
0.82), a clean illustration that linear beats nonlinear where the data lie in a low-dimensional
linear subspace. Lane correction carried into T11e's caption: oracle fits are not free of budget
exits (12/384 for ns203, 18/384 for ns204 hit the LM cap), so oracle values are upper bounds;
medians and both verdicts stand. The last NS PENDING marker is gone; ns204 leaves the in-flight
register. The collapse paragraph now says "at both K = 16 and K = 32" with both ratios as macros;
the structural-condition sentence is unchanged. **Figure 2 (architecture) moved to Appendix B for
the page budget** — the main text keeps the pointer; this reverses the earlier §3 placement.


### Round-2 review, item 1 (2026-09-17): the operator premise, overriding the old wording

The abstract's opening premise, the intro (two places) and the conclusion said neural operators
"offer no way to tune either at deployment" / "one fixed operating point". §5.1 of this paper
withdraws that (T14c: fno-large's resolution knob works, 2.12× own speed at 1.14× own error; the
U-Net and Transolver arms are degenerate). Rewritten with the smallest true change: operators
deliver one point per trained model; at deployment only the evaluation grid can be changed, which
moves cost but not what the model can represent. The user's "keep the old wording" is overridden
here because the old sentence is false by the paper's own table; the coordinator will tell the user.


### Round-2 review, item 3 (2026-09-17): the L-shape reframed

Overrides the earlier "cost-only win" framing. (a) The L-shape is linear Poisson and now
illustrates the no-fast-transform case, not the structural condition; the condition is restated
as a trade *between rungs* (nonlinear residual + manifold beating POD), separate from whether a
reduced solve wins on cost at all (no fast transform). (b) POD-128 is cheaper than the head at
both meshes and beats the head's margin against the direct solve (2.94x / 9.06x vs 2.77x / 7.60x);
the head is non-dominated only because it is more accurate (2.12 % vs 2.45 %); both printed. (c)
same-job CG at 1e-2 is cheaper than the sparse direct solve at 512² (29.13 vs 36.51 ms) and more
accurate than every reduced arm; margins are now given against the cheapest same-job full-order arm
(6.07x at 512²) as well as against the direct solve; the abstract uses the cheapest-arm margins.
M6: the "optimistic end of a range" caveat now says what it is (best-found reconstruction
degrades 2.5x from 32 to 461 sources; no validation solve sweep). M7: the head cost trend is
marked cross-job and the head rows are printed at every mesh (new main-text table).


### Round-2 review, item 4 (2026-09-17): seeds and the two ladders separated

The abstract no longer splices the scheduled ladder (monotone on three seeds) with the fixed-M
ladder (meets the bar, one checkpoint). §6.3 and limitation (i) now print: knob bar met on 2 of 3
seeds, which is b-seeds DESIGN C4's pre-registered pass; seed 3 fails on convergence at q=256
(budget exits), not on span (its spans quoted); and the incumbent checkpoint beats all three
fresh seeds at q = 16–128 on the evolved metric (Table 30), i.e. the hero is a favourable draw.


### Round-2 review, item 8 (2026-09-17): ratio provenance and the pre-registration, corrected both ways

"4.48x -> 1.82x" is now two same-job facts with their GPUs named (A100 at 256², H200 at 1024²),
no arrow, not called a crossover. §6.3 now says M = 1088 IS pre-registered (b-qxm DESIGN §3 grid
table and §6 headline rule); the earlier "not pre-registered" understated the lane. The bar is
quoted as DESIGN §6 states it (monotone on the evolved metric, every rung converged, error span
>= 2x) with the two generator-only clauses (>= 3 non-dominated points, cost span >= 2x) labelled
"as implemented, inherited from an earlier campaign".


### Round-2 review, item 9 (2026-09-17): provenance leaks closed

The three bars (0.116, 0.06, 2.0) now live in `paper/bars.json` with their DESIGN sources and
commits, read by `gen_tables.py` (sha256 recorded in provenance.json; sources printed in the
`tab:eqrules` caption). Word-form numbers replaced by macros or removed: "three to four" (bank/POD
ratio), "four times the primary bar", cohort counts (Table ref), "eight-case" (cohort-name macro),
"three cheapest rungs" and "top two rules" (q lists). `\provLshapeJob` now keys on the printed
bank/head rows (training job 3783786). The mesh sentence uses the complete-query (host-to-host)
ratio, generated from the printed rows; every cost column is headed "device ms" or
"complete-query ms" and §5 defines both. The wave device-ms ratios are labelled as such.


### b-panel 512² (bpn401, job 3805065) folded in (lane commit d2135501)

0 of 31 admissible reduced subjects non-dominated at 512² (frontier = full-order Newton alone; the
FNO is not on it); so 0 / 0 / 5 at 256² / 512² / 1024² and the flip lies between 512² and 1024².
Same-job ratios: cheapest reduced / cheapest FOM 4.48x (256²) and 2.92x (512²) on the same A100
model — a shared-hardware trend; 1.82x at 1024² (H200) stated separately. Physical column at 512²:
ladder 3.76 -> 2.76 % over a converged same-grid error of 2.70 %; all 39 reduced subjects above it
(closest pod512 +0.0185 pp); the one FOM setting below it (nt1e-2_dt005) flagged as under-resolved
cancellation. Quadrature: transferred rules labelled "transferred, primary/not primary at 512²;
source <verdict>"; the replication-selected q=128/256 transfers pass primary at 512² though their
sources are single-draw; ladder-rule transfers at q>=64 do not. Gate matched_rule_files_bitwise
reported FAILED (transfer refits on its own draw; q=32 source certifies primary in one transfer and
secondary in the other, ~8.7x in rho_max), one sentence in §6.3 and a Limitations line; no
falsification clause triggered. Note: the q=256 eqtopxfer dense-twin ratio is printed at tolerance
1e-6 (21x); the 33x in the lane message is the 1e-3 arm. bpn401 leaves the in-flight register.


### ns2d ns301 (job 3808493) folded in (lane commit f63de724)

Head-only retraining on the frozen K=16 bank: held-out oracle 27.49 -> 20.69 -> 19.83 % (plain) for
128/256/512 trajectories; generated gains 25 % for the first doubling and 4 % for the second on the
plain recipe (13–25 % / 4–8 % across the three recipes — the "25–35 %" in the lane message is not
what the ratios give; the paper prints the generated values); weight decay + early stopping and the
4x smaller head move the 512 value by <= 2.2 %. Pre-registered rule: "ambiguous" (slopes -0.236 /
-0.215 / -0.148 vs band -0.25 to -0.10), not upgraded; the flattening leans head-limited; caveat
that the bank saw all 512 trajectories. One sentence in §6.5, T11f in the extended-results appendix.
Register: ns301 landed; ns302–ns304 running. The band's DESIGN source is in bars.json.


### b-seeds sealed cohort (job 3804465) folded in (lane commit be9415ab)

The sealed cohort was opened once, after every choice was frozen. All four checkpoints (incumbent,
seed1–3) are monotone on both metrics and pass the knob bar (3 of 4 on the lane's per-checkpoint
`knob_bar_passes`, seed2 failing on its unconverged q=64 rung; the prose says "meets the knob bar"
for the ladder property and reports the unconverged rung separately). Sealed q=0 → q=256: incumbent
10.11 → 0.68 % (375 → 6726 ms), seeds 2.15/2.83/3.43 → 0.61/0.59/0.66 %. Seed-mean sealed/dev
ratios 1.40 (q=0) down to 1.08–1.24; worst 1.40 at q=0.

Pre-registered F2 applies: C2 and C2n fail at q=0 for the incumbent alone (ratio 5.35), caused by a
converged wrong-branch solve on sealed case 4 (gradient exit, zero budget exits, 10.8 % at the first
output time; best-found on that case 2.55 %). Consequence written into the paper: the sealed numbers
are the headline for the scheduled ladder; the development seed table is seed variability; the
incumbent's development q=0 value 1.8890 % is never quoted as a generalisation figure — it appears
only in the ladder table's development column beside the sealed column, in the seeds paragraph as
"sealed against ... in development", and in the extended-results POD comparison labelled as
development-cohort. Limitations (i) now states the wrong-branch cold start at q=0 as a failure mode;
q>=16 removed it in this instance and the sentence claims nothing more.

C3 fails: seed2 sealed q=64 exits on budget on case 4 (stationarity 2.86e-6 vs 1e-6) and seed3's
development q=256 rung — both marked unconverged in T13/T12 and reported as run, not tuned.
C1 2 of 3, C4 2 of 3, TR 3 of 3, F3 unchanged (no). Abstract: "on three training seeds and on a
sealed cohort (top-rung error 0.59–0.68 % over 4 checkpoints)" via macros; the rule "no performance
ratio in the abstract until T12 and T13 exist" is lifted. The fixed-M ladder (b-qxm) is a different
object and unchanged; one clause says so. Register: 3804465 removed. Page budget kept by wording
trims in §6.2–6.5 and Limitations (v) (the "same-job full-order setting more accurate" clause stays
in §6.2 where it is argued). Lane retraction recorded for the lab log, not the paper: the incomplete
lab-log entry at 84a537ff.


### ns2d ns304 (job 3808502) folded in (lane commit 46650a1e) — exploratory, not phase 3

Exploratory q-ladder on the ns204 K=32/R=512 manifold, which failed the phase-2 held-out oracle
gate; fixed M=2176, 8 development cases, 3 timed reps, same-job matched POD-LSPG and FOM tolerance
ladder. Labelled "exploratory after a failed phase-2 gate" on every table, caption and sentence; not
described as a phase-3 result anywhere. Generated values (per cent): worst evolved 68.77/70.77/
61.51/56.55/33.05/6.01 at q=0/32/64/128/256/512, median 41.39 -> 2.44, ms 13388 -> 52592, budget
exits 0; manifold layer 10.55 -> 0.2254 (the bank floor) reached only at q=R; residual energy
captured 26/44/68/92/100 % (the lane's field). POD-LSPG at k'=K+q: 54.63/35.51/27.73/17.20/9.54/
4.17 % at 212/431/833/2398/9964/53903 ms. Sentences in §6.5: the correction rank closes the manifold
gap toward the bank floor, fully only at q=R where the head no longer matters; POD-LSPG at the same
dimension is more accurate at every rung and cheaper at all but the top, and no neural rung is
non-dominated — the falsification clause's named outcome for this head.

Two departures from the coordinator's wording, both generated: (a) POD is cheaper at 5 of 6 rungs,
not every rung (q=512: 53.9 s vs 52.6 s), so the paper says "all but the top"; (b) the solve-layer
ratio printed is median-over-median, 2.9–10.8x, not "4–10x". Provenance row T11g/T11h: job 3808502,
NVIDIA A100 80GB PCIe, source commit 31e0846f, ckpt_K32_R512 hashed in result.json. Register: ns304
landed, ns302–ns303 running. Page budget: main text ends on page 9 after wording trims in §6.5 and
the Limitations.


### ns2d ns303 (job 3808498) folded in (lane commit 5ea1cc30)

The K=16/R=256 recipe on a lower-dimensional family (NMODES=3; intrinsic dimension 8 instead of
14, both parsed from the lane's DESIGN §A12). All gates pass except H-ORACLE: oracle median 5.28 %
vs POD-16 7.37 %, ratio 1.39 (bar 2.0); held-out/training gap 3.8; POD-16/oracle 1.83 at t=0, 1.44
on evolved times; bank floor median 0.277 %, B-FLOOR ratio 1.03; H-TRAIN 1.39 %; 4 budget exits.
Against ns203 the errors fall 3.3x (POD-16), 3.8x (oracle) and 4.4x (bank floor); the paper prints
that range rather than "about four-fold". Claim added to §6.5, generated: the head's advantage over
POD-K does not grow when the manifold gets easier (1.19 -> 1.39; gap 4.0 -> 3.8), and across
K=16/32, family dimension 14/8 and 128–512 trajectories this head class does not beat POD-K by the
pre-registered 2x on held-out decaying 2D Navier–Stokes; ns302 (4x data, job 3808495) is the last
arm and stays in the register. T11e caption names the third block, the drop factors and the
budget-exit counts. No phase-3 claim; the exploratory ns304 paragraph is unchanged and still
labelled. Provenance: provNsJobs now lists 3808498 (K=16, R=256, family dimension 8); GPU "A100
(pax105)" from DESIGN §A12 (the lane's result.json records only the device index). Page budget
kept by wording trims on pages 8–9 (no claim, number or reference removed).


### b-lowvisc folded in as an appendix cell (lane commit df92e40d; panel job 3817807)

Not a headline; every number carries the pre-registered F4 caveat (training mesh under-resolved:
the converged discrete operator sits 19.3–20.9 % from the 4096² reference, 5.17x the incumbent
cell's discretisation error), so the cell is evidence about reduced models against each other on
the same discrete system only. Claims added (§6.5, two sentences; Appendix F.6; Limitations (ii)):
at ten-fold lower viscosity the linear floors collapse 12–19x (POD-512 12.09x, trained bank floor
19.00x, 0.3918 → 7.4455 %) while the head degrades 3.3–4.5x (solved 2.5629 → 8.3933 %, best-found
2.5447 → 11.3934 %, the latter an upper bound: solved/best-found 0.74); every neural rung beats
every POD-LSPG rank and q>=64 sits on the reduced-only frontier, so the manifold's advantage
appears where the Kolmogorov width is worst; but nothing reduced is on the full-order frontier
(criterion P fails; cheapest dominating full-order setting nt1e-2_dt01 at 14.0 ms / 3.90 %), the
fixed-M ladder spans only 1.36x (9.05 → 6.67 %, 557 → 1810 ms; monotone, converged, knob bar not
met) and the mesh is under-resolved. F2 does not fire. Departures from the lane message, all
generated: the crossover against POD-LSPG is at q=0 (every rung), against the free bank's own
solve at q=128, and the reduced-only non-dominated neural rungs are q=64/128/256 — written as
above rather than "from q=64 up every neural rung beats every POD-LSPG rank and the bank's own
solve". Lane retraction (not the paper's): a hand-written sentence in the lane report had named
nt1e-3_dt005 as the cheapest FOM beating every reduced subject; the generated value nt1e-2_dt01
is what the paper prints. Register: lvt01 (3804337) removed; ns302 remains. Page budget kept by
wording trims on pages 8–9 (no claim or reference removed).


### Readability pass applied (2026-09-18; on 0366a6b3) — abstract choice open

Sentence splits and plainer connectives from the coordinator's readability pass were applied to the
abstract, introduction (incl. contributions), the opening paragraph of each results subsection, the
Limitations and the conclusion; no macro, number or claim changed. The working abstract is now the
pass's **Abstract A** (<= 250 words) with three edits: the first-use glosses required by the same
message (EQ, NNLS, ladder, sealed cohort, same-job) and "on one checkpoint" restored to the
fixed-test-count clause (the pass had dropped that qualifier); the operator names in the first
sentence were shortened to `\citep{Li2020FNO,Lu2021DeepONet}` to stay within 250 words. Final count
247 words (whitespace tokens of the LaTeX source, macros and citations as single tokens).

Skipped from the pass: (1) the optional "What we do not claim" rewrite (the six-fragment list is
shorter and parallel; kept for the page budget); (2) Limitations (iv) taken with one precision edit,
"one transfer-draw spread in rho_max is 8.7x" rather than "the transfer-draw spread" (it is one q=32
source's spread, not a general figure); (3) the intro machinery list and 6.1 S1–S2, 6.2 S1/S3, 6.4 S2,
Limitations (v) were "leave as is" in the pass itself. Contribution item 3 takes the pass's subject
"Three things make the framework practical" — the same claim as that item's bold heading, not a new one.

**Alternative 1 — the abstract before this pass (397 words), verbatim:**

```latex
Neural operators such as Fourier Neural Operators \citep{Li2020FNO} and
DeepONets \citep{Lu2021DeepONet} deliver one (accuracy, speed) point per
trained model; at deployment only the evaluation grid can be changed,
which moves cost but not what the model can represent. We present a non-linear manifold reduced order
model (NM-ROM) for elliptic, parabolic and hyperbolic PDEs that exposes a
family of accuracy/cost operating points from a single trained
decoder, controlled at inference time by one primary knob, the rank $q$ of a
linear correction the solver may switch on, and three solver-side knobs
(iteration cap, tolerance, empirical-quadrature sample count). The framework combines a matrix-free least-squares Petrov--Galerkin
projection of the discrete residual onto fixed weak tests
\citep{Bradbury2018JAX}, NNLS-based EQ hyper-reduction
\citep{Hernandez2017EQ,YanoPatera2019LPEQ} validated on the states the
solver actually reaches, exact boundary enforcement inside the decoder,
and a separable decoder --- a per-node spatial bank from a
Fourier-feature coordinate network \citep{Tancik2020FourierFeatures}
times a small neural head with a linear skip, plus nested correction
directions. Across 2D Burgers, Poisson, heat and waves, the trained-once family's
scheduled ladder is monotone in $q$ on Burgers on three training seeds
and on a sealed cohort (top-rung error
\nSealedAllTopMin--\nSealedAllTopMax\,\% over \nSealedCheckpoints{}
checkpoints), and its fixed-test-count ladder, where $q$ is the only control,
meets a bar fixed before any run on one checkpoint, on the same-grid
error: against a fine reference every rung's
error is at most $\nPanelRungRefOverDiscMax\times$ the mesh's own
discretisation error, so the knob moves the reduction error, not the
physical error, at the meshes we ran; its quadrature rules are validated on reachable states, not by their
fitting residual, and confirmed on re-draw at $q\le32$ only;
and its cost results are same-job: on an L-shaped Poisson domain, where
no fast transform applies, the head is
$\nLshapeNeuralCheaperVsCheapestTwoFiftySix\times$ and
$\nLshapeNeuralCheaperVsCheapestFiveTwelve\times$ cheaper than the cheapest
full-order solve at $256^2$ and $512^2$, and plain POD-128 cheaper still
for $\nLshapePodOverHeadErrFiveTwelve\,\%$ more error; at $1024^2$ on
Burgers the cheapest rungs are non-dominated on evolved times. Nothing reduced is on the frontier at $256^2$ or $512^2$, where a
tuned full-order solver (and at $256^2$ a neural operator trained on the
same data) is cheaper and more accurate; and on
Poisson, heat and waves the family collapses to a linear model, which
says when the nonlinear manifold is worth having.
```

**Alternative 2 — the pass's Abstract B (~200 words; one line shorter than A in the PDF), verbatim
from the pass, without the glosses A now carries:**

```latex
Neural operators \citep{Li2020FNO,Lu2021DeepONet} deliver one (accuracy, speed) point per trained model; at deployment only the evaluation grid can change, moving cost, not representation. We present a non-linear manifold reduced order model (NM-ROM) for elliptic, parabolic, hyperbolic PDEs, exposing a family of accuracy/cost points from one trained decoder, controlled by the correction rank $q$ and three solver-side knobs. The framework combines a matrix-free Petrov--Galerkin projection \citep{Bradbury2018JAX}, EQ hyper-reduction \citep{Hernandez2017EQ,YanoPatera2019LPEQ}, exact boundary enforcement, and a separable decoder: a coordinate-network bank \citep{Tancik2020FourierFeatures} times a small head with a linear skip and corrections. Across 2D Burgers, Poisson, heat and waves, the scheduled ladder is monotone in $q$ on Burgers (three seeds, sealed cohort top-rung error \nSealedAllTopMin--\nSealedAllTopMax\,\% over \nSealedCheckpoints{} checkpoints) and meets a pre-registered bar at fixed test count, with error against a fine reference within $\nPanelRungRefOverDiscMax\times$ the discretisation error, quadrature confirmed only at $q\le32$. Same-job, on L-shaped Poisson the head is $\nLshapeNeuralCheaperVsCheapestTwoFiftySix\times$/$\nLshapeNeuralCheaperVsCheapestFiveTwelve\times$ cheaper than the cheapest full-order solve at $256^2$/$512^2$, POD-128 cheaper for $\nLshapePodOverHeadErrFiveTwelve\,\%$ more error. Nothing reduced is on the frontier at $256^2$ or $512^2$: a tuned full-order solver is cheaper and more accurate. On Poisson, heat and waves the family collapses to a linear model, showing when the nonlinear manifold pays off.
```


### ns2d closed (lane commit 9830d202; ns302, job 3808495) — last data fold-in

The K=16/R=256 recipe at 4x the training data (2048 = 512 gated + 1536 extra from seed 20260920,
same 14-dimensional family). All gates pass except H-ORACLE: oracle median 16.66 % vs POD-16
24.25 %, ratio 1.46 (bar 2.0); 6/384 budget exits; B-FLOOR 1.20; H-TRAIN 12.39 % against 5.03 % at
512 trajectories; held-out/training gap 1.3 against 4.0; POD/oracle 0.52 at t=0, 1.39 evolved.
Claims now final, all generated: across K=16 and 32, family dimension 14 and 8, and 128–2048
trajectories this head class never beats POD-K by the pre-registered 2.0x on held-out decaying 2D
Navier–Stokes (ratios 1.15–1.46 over the four phase-2 arms at 256²); with 4x data the gap closes
from 4.0 to 1.3 because the head no longer fits the training data either, so the limit moved from
generalisation to capacity at the same head. The 1.25–1.35 prediction scored by the lane is not
quoted (lane-internal). B-DATA passed by the pre-registered value path (hash mismatch on the node,
1e-16 on the archived trajectories): one sentence in the T11e caption. Register emptied; the
appendix in-flight table reads "none". Conclusion adds "another head class for Navier–Stokes".
With this the paper is complete on the evidence side; what remains open is the user's headline-metric
choice and the abstract choice (A now; B and the previous abstract recorded above).


### Reader map added (2026-09-18): "Experiments at a glance" (Table 1) and appendix float numbering

No claim or number changed. Table 1 (generated `T00_glance`, §5) lists every experiment with the
question it answers, PDE/meshes, the job count (distinct job ids of the named provenance rows) and
lane, and the tables/figures/appendix sections that hold its numbers; one sentence at the top of §6
points to it. Appendix floats are numbered by appendix section (Table E.3, Figure B.1) so that
main-text pointers to appendix tables are visibly pointers; the main-text tables are 1–4 in reading
order (the "Table 12" the user saw on page 8 was a \ref to the appendix rank-vs-test-count table,
not a misplaced float). The glance table pushes the end of the main text onto page 10, allowed for
this table only.


### Glance table moved to Appendix A (2026-09-18)

The ICLR 2027 kit's 9-page limit at submission forbids page 10, so Table "Experiments at a glance"
is now Appendix A / Table A.1 (unchanged), with the §6 pointer sentence kept and reworded. Three
lines recovered by wording only; no claim, number or reference removed. Main text ends on page 9.


### Secondary comparison against unpreconditioned CG (2026-09-18)

Added at the user's request as a secondary comparison, never a headline: Table T21 in the extended
results and one sentence in §6.5. Generated, same job only: on Poisson the reduced rungs (q=0–256 and
the linear top rung) are 3.7–23.4x faster than CG 1e-6 (256²: 3.7–11.8x; 1024²: 15.1–23.4x), while
the DST direct solve is faster than every rung by 1.3–4.0x — which is why the paper compares against
the transform. L-shape rows use CG at the lane's tightest tolerance 1e-10 (no 1e-6 arm), SuperLU and
the CPU IC(0)-PCG; head/CG 2.9–14.9x. Heat omitted (committed record is raw per-invocation timing of
an earlier decoder family; not re-aggregated) and Burgers has no CG comparator; both stated in the
caption. §5 baselines: "the earlier comparison against plain CG is kept in Appendix G for
continuity." Page budget: wording trims in the NS and Limitations paragraphs and the conclusion
(some readability splits re-tightened: "(vi) No convergence or quadrature-error theory", "Next: ..."
list form); no claim, number or reference removed.


### NS solve-layer ratio corrected from 2.9–10.8x to 1.4–2.5x (statistic mismatch, lane §A14; ns2d 2d70f36a)

The ns304 "solve layer 2.9–10.8x above the manifold layer" (printed as median-over-median) divided a
median over all 48 states including t=0 by a median over cases of the worst evolved time. On the
matched statistic (median over cases of the worst evolved time on both sides; summary key
threelayer.solved_over_manifold_matched) solved/manifold is 2.51/2.31/2.20/1.91/2.04/1.40 at
q=0/32/64/128/256/512: the paper now prints 1.4–2.5x, decreasing with q, and keeps the old figure
only as a superseded secondary (2.9–10.8x, med48) with the reason. New generated rows: the loss
accumulates along the trajectory (per-time ratio at q=0: 1.44, 1.66, 2.19, 2.89, 3.85 at t=0.2–1.0);
enstrophy ROM/reference at t=1 median 1.043 at q=0, 0.990 at q=32, 0.95–0.97 at q=64–256, 0.999 at
q=512, POD-32 control 1.026. The same-solver linear-control ratios quoted only in the lane's §A14
(POD-32 1.49x, POD-512 1.26x, bank-512 1.37x) are outside the archive and are NOT in the paper. T11g
now shows the matched manifold/bank layers and a solved/manifold column. Structural reading
unchanged: POD-LSPG more accurate at every rung and cheaper below the top; no neural rung
non-dominated. Burgers wording checked: the head-ablation "solver layer costs 0.018 pp" is an
all-times gap pinned by the t=0 compression term; the §6.4 sentence now says so ("on the all-times
metric, where the t=0 term dominates"); no evolved-times gap exists as a generated number, so none
is typed. No main-text sentence had quoted the old NS ratio.


### b-panel admissibility corrected to DESIGN §5 (lane §A13): 1024² frontier 5→3 reduced arms, ratio 1.82→2.27x; 256²/512² unchanged (2026-09-19)

Every b-panel pin moved from `d2135501` to `25434a27`. The lane's audit had evaluated its
convergence rule against each arm's *own* gradient tolerance, so the 1e-3 arms were flagged
converged and entered the admissible frontiers and ratios; DESIGN §5 as written requires 1e-6
stationarity at every step (or a residual-rule exit). The lane's §A13 makes §5 primary:
`admissible` (= `admissible_design5`) is the flag the paper reads, `converged` is now
`converged_design5`, the `nondominated_*_admissible` / `_reduced_only` rows are §5, and the
pre-A13 flags survive as `*_own_gtol` and define nothing. The generator now reads
`converged_design5` (three places) and the figure generator does too; nothing else changed.

What moved, all generated: admissible reduced 39→27 at 256², 31→23 at 512², 20→16 at 1024²;
reduced subjects on the (GPU ms, worst evolved) frontier at 1024² 5→3 (survivors q0/q16/q32
eqxfer at 1e-6, i.e. the 1e-6 twins of the dropped 1e-3 arms); cheapest admissible reduced /
cheapest same-job FOM at 1024² 1.82→2.27x and vs `fft_tight` 0.153→0.191x. The 256² and 512²
ratios are unchanged (4.48x / 2.92x) because their cheapest admissible arm was already a 1e-6
`fast` kernel, and nothing reduced is on either frontier under either flag. The loose-tolerance
ladders' `all_converged` verdicts flip yes→no under §5 (not quoted in the text). Figure 2B now
plots 36 admissible subjects at 256² instead of 48; its frontier (FNO, five Newton settings, the
transform) is unchanged.

Claims affected: §6.1's 1024² count and ratio were already macros and updated themselves; the
opener now states the generated count; the 1024² paragraph drops the arm list's tolerance
qualifier because §5 now defines it. New: one sentence in §5 stating the admissibility rule, an
``adm.\ (own tol.)'' column beside ``adm.\ (\S5)'' in the three appendix panel tables with the
correction and the commit in the \ref{tab:panel-all} caption, an "Admissible" glossary entry,
and a clause in §6.3 saying the 1e-3 cost saving buys arms that are not admissible. The abstract
quotes no 1024² count or ratio (verified); Limitations does not mention the 1e-3 arms. Page
budget: three lines recovered by wording (a §6 sentence that restated §5's baselines list, the
duplicated "make the framework practical" in contribution 2, and the 1024² paragraph's opening
clause); no claim, number or reference removed. Main text ends on page 9.
