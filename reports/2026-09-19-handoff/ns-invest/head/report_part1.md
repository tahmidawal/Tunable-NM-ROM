# Why the auto-decoder head does not generalise on 2D decaying Navier–Stokes — the head angle

Investigation of lane `ns2d` (worktree `worktrees/2026-09-17-ns2d/experiments/ns2d`, commit `9830d202`), read-only.
Scratch directory: `/tmp/claude-1002/-home-tahmid-Dev-pod-ae-nmrom-Tunable-NM-ROM-Claude/6e5fe858-5d6b-4b34-a1bd-df699b1d4032/scratchpad/ns-invest/head/` (this file, the scripts `gen64.py`, `common.py`, `analyze_a.py`, `analyze_b.py`, `analyze_burgers.py`, their JSON/log outputs, and the 64² regenerated cohorts `data64.npz`).
Every number below is either quoted from the lane's `result.json` / `oracle_N*.npz` artifacts or generated by the named script; nothing is typed from memory.

Sections: 1 differences from Burgers · 2 evidence from the artifacts (with scripts) · 3 hypotheses ranked with tests · 4 recommended design and pre-registration · 5 training on reference solutions (coordinator addition) · glossary.

---

## 1. Differences from the Burgers recipe, line by line

Sources: NS `ns2d_decoder.py` (bank/head/`train_autodecoder`/`make_lm_fit`/`oracle_fit`), `ns2d_phase2.py` (data, POD, floors, gates), `ns2d_headfit.py` (ns301 head-only refit), `sep_common.py` (`init_mlp`/`head`/`train_autodecoder`); Burgers incumbent = `b-seeds/DESIGN.md` §4.1 (stage A `sep_burgers_r3.py`, stage B `sep_coeff_extract.py`, stage C `sep_hfit.py` + `sep_hfit_run.py` arm `mid`), `mr-burgers2d/engines.py` (family), `separable-decoder/HFIT.md` (Burgers head study).

| # | item | NS (ns203 / ns204; ns301 head-only; ns302/303) | Burgers incumbent (`sep_hfit_dense_mid_N256`) and the b-seeds retrains | matters for the head? |
|---|---|---|---|---|
| 1 | head architecture | `sc.head`: SiLU MLP **512×3** + linear skip `h_lin` (K×R), `ns2d_phase2.py:42-43` via `ARCH` (`H_HIDDEN`, `H_LAYERS`); ns301 `reg_small` 128×2 | stage C: `sep_hfit.init_head` SiLU MLP **512×2** + linear skip (`hfit_spec {'hidden':512,'layers':2}` in `sep_hfit_seed1.pkl` cfg); stage A head 256×2 | no — depth 3 vs 2, same class, same init (`sqrt(2/fan_in)`, `h_lin` 0.3·N(0,1)); ns301 showed 128×2 changes the held-out number by 1 % |
| 2 | latent dimension | K=16 (R=256) and K=32 (R=512) | K=16 (R=512) | no |
| 3 | bank features | integer Fourier frequencies in `[-6,6]²`, `n_ff=64`, no boundary factor, columns made mean-zero on the grid (`ns2d_decoder.py:35-63`) | Gaussian `B·4.0`, `n_ff=128`, `bc_poly` wall factor (`sep_common.py:84-120`) | no — bank floor 0.4–1.2 % median on NS is not the limit |
| 4 | bank width / rank | `G_HIDDEN=2R` after §A4 (512 / 1024), 2 layers | `g_hidden=1024`, 2 layers, R=512 | no (identical rule) |
| 5 | training stages | **one** joint auto-decoder stage over (bank, head, codes): `D.train_autodecoder`, 100 000 **full-batch** Adam steps on all 13 312 states (`BATCH=0`), warmup-cosine `lr 1e-3`, `lam_orth 1e-4`, global-norm loss `mean(err²)/u_ms`, **no** weight decay, **no** EMA, **no** point subsampling, last iterate (`ns2d_decoder.py:69-133`); ns302 used `BATCH=13312` of 53 248 rows | **three** stages: A joint auto-decoder on **16 384 picked states** of 576 trajectories (all `t≤5` states + random rest), 300 000 steps with **4 096 random points per step** (`p_sub`), AdamW `wd 1e-5`, EMA 0.999, last 10 000 steps on all points; B project **131 072 states of 4 608 trajectories** on the frozen bank; C cold head refit in the **whitened coefficient space**, 200 000 steps at **batch 4 096**, per-snapshot (`snap`) relative loss (`b-seeds/DESIGN.md` §4.1; `sep_hfit.fit`) | partly — ns301 reproduced stage C on the frozen ns203 bank (whitened space, 100k steps, full batch, wd/early-stop arms) and got the same held-out number (19.8 % vs 20.2 %); the recipe mechanics are not the cause |
| 6 | loss normalisation | global (`/u_ms`) in both stages | stage C `norm='snap'` (per-snapshot relative); HFIT `glob` vs `wide` arms differ by 10 % | minor |
| 7 | **trajectories seen by the head** | **512** (ns203/204/303), 128/256/512 (ns301), **2 048** (ns302) | **4 608** (576 canonical + 4 032 extra) — the corrected fact in the tunability handoff §3.5 | **yes** (§2.3) |
| 8 | **states per trajectory / time sampling** | 26 states (every 20 of 500 steps, `OUT_EVERY=20`), horizon T=1 | 51 states (every step of 50, `dt=0.005`), horizon 0.25; stage A pick biased to `t≤5` (21 % of picked states) | secondary |
| 9 | **family / input dimension** | 12 i.i.d. N(0,1) amplitudes of 6 Fourier modes, rescaled to `U_rms=1` (an 11-sphere) + `log ν ∈ [-3,-2]` + t: **13 intrinsic dims + t** (`ns2d_fom.params_draw/initial`); ns303: 6 amplitudes → 7 + t | `(cx, cy) ∈ [.15,.85]², w ∈ [.05,.2], a ∈ [.5,2], ν log-uniform [.01,.1]`: **5 dims + t** (`engines.py:20-25`) | **yes** — 512^{1/13} ≈ 1.6 samples per axis vs 4608^{1/5} ≈ 5.4 |
| 10 | **linear baseline strength** | POD-16 median 24 %, POD-32 14 %, POD-256 3.3 %; 89 % of snapshot energy in 16 POD modes (`artifacts/ns203/result.json: pod.energy_fraction`); at t=0 the IC family **is a 12-dim linear subspace** | POD-LSPG k'=16 61.7 % solved / 28.7 % evolved (`b-ladder-top` report); best 16-dim linear subspace inside the bank on test states: §2.6 | **yes** — the H-ORACLE bar is *relative* to POD-K |
| 11 | dynamics | decaying flow: the nonlinear term generates harmonics of six low modes and viscosity removes them; the state never leaves a low-mode neighbourhood (POD-256 floor 3 %) | translating/steepening Gaussian bump: transport, slow Kolmogorov-width decay (POD-128 still 10 %) | yes — where a nonlinear manifold pays off |
| 12 | symmetry | torus translations (2-dim) and D4 act on the family; the auto-decoder does not use them | Dirichlet walls break translation | secondary |
| 13 | oracle protocol | `oracle_fit`: LM in the whitened bank metric, **8 starts** from the nearest training codes (field score), budget 300, `gtol 1e-6`; 3–5 % budget exits (§A8) | stage-C `sep_hfit.oracle`: mean code + training-only encoder inits, 120–150 LM iterations; ladder jobs: varpro block LM | no — the NS oracle is if anything more generous |
| 14 | code initialisation, schedule | `0.1·N(0,1)`, warmup-cosine, `lr 1e-3` | same | no |
| 15 | reference for errors | same-grid converged FOM (`ntol 1e-11`), errors normalised by `‖ω(0)‖` | same-grid `fft_tight`; a 4096-interval reference only in the ladder's "vs ref" column | §5 |

Net: the head class, the optimiser and the oracle are the same; what differs is the **data budget per intrinsic dimension** (rows 7, 9), the **strength of the linear competitor** (rows 10, 11), and the resulting difficulty of the interpolation asked of a free-code (auto-decoder) chart. Section 2 quantifies each.
